# 月度开源热点 → 本地 Agent 行为映射

Triggers: trending,榜单,热点,开源,社区,月度,github,monthly_trend_map,monthly trend map,monthly-trend-map,月度开源热点,本地,Agent,行为映射,月榜,本月热点

---

**何时使用**：用户意图与「月度开源热点 → 本地 Agent 行为映射」相关，或 Triggers 中任一词命中时**应**挂载；勿等待用户说出技能 id。

## 执行步骤
1. **技能与工程规范**：把「触发词 + 短 playbook」当作默认输出结构；改代码前先对齐仓库既有风格
2. **终端/IDE 里的编码代理**：优先用工具拿事实，少讲「你应该手动怎么做」
3. **代码库级上下文（MCP 思路）**：大范围改动前先 `list_files` / `read_file` 定位入口，再小步改；避免一次性大块重写
4. **多代理/多角色**：复杂任务走 `run_task_orchestration` 或显式分角色（规划/实现/审查），每段输出要有可验证产物
5. **Token 与信号**：长日志先压缩结论再给片段；同一事实不重复堆叠
6. **可观测与 SRE 气质**：失败时保留报错原文片段、假设、复现步骤；把「告警」翻译成可执行检查项
7. 无工具/无读取就声称「已完成」或编造文件内容
8. 把 `.env`、token、密钥写入聊天或记忆

## 避免
- 无工具/无读取就声称「已完成」或编造文件/命令输出。
- 把 `.env`、token、密钥写入聊天或长期记忆。

## ONYX 对接
- 工具/配置 `list_files`
- 工具/配置 `read_file`
- 工具/配置 `run_task_orchestration`

## 自测用语（习惯体检 / 人工抽检）
- （自然语）帮我处理「月度开源热点 → 本地 Agent 行为映射」相关的事
- [skill:monthly_trend_map] 执行一步可验证操作
