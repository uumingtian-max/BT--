# 多角色编排与交接契约

Triggers: 多模型,编排,协作,复杂任务,拆解,子任务,orchestrate,总控,orchestration_handoff,orchestration handoff,orchestration-handoff,多角色编排与交接契约,多模型协作,角色分工,交接

---

**何时使用**：用户意图与「多角色编排与交接契约」相关，或 Triggers 中任一词命中时**应**挂载；勿等待用户说出技能 id。

## 执行步骤
1. 触发 `run_task_orchestration` 的场景：跨多文件/多步骤、需要独立审查、或用户明确要求「多模型」
2. 交接时写清：**输入假设**、**已完成**、**阻塞点**、**下一步最小动作**；避免把上一段整段日志原封扔给下一角色
3. 规划者输出应是「可验证清单」，实现者输出应是「具体路径 + 代码/命令」，审查者输出应是「风险列表 + 建议测试」
4. 若子任务失败：汇总里必须突出失败子任务与错误摘要，不要粉饰为全部成功
5. 无工具/无读取就声称「已完成」或编造文件内容
6. 把 `.env`、token、密钥写入聊天或记忆
7. 工具 `run_task_orchestration`
8. （示例）用户会用自然语言提到「多角色编排与交接契约」相关任务

## 避免
- 无工具/无读取就声称「已完成」或编造文件/命令输出。
- 把 `.env`、token、密钥写入聊天或长期记忆。

## ONYX 对接
- 工具/配置 `run_task_orchestration`

## 自测用语（习惯体检 / 人工抽检）
- （自然语）帮我处理「多角色编排与交接契约」相关的事
- [skill:orchestration_handoff] 执行一步可验证操作
