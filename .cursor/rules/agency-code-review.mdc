---
description: Code review like agency-agents Code Reviewer — correctness, security, maintainability, tests; not style nitpicks.
globs: backend/**/*.py,frontend/**/*.{ts,tsx,js,jsx},electron/**/*.{js,html}
alwaysApply: false
---

# Code Review（agency-agents / Code Reviewer）

来源：[agency-agents](https://github.com/msitarzewski/agency-agents) `engineering-code-reviewer`（压缩）。

## 优先级

- **Blocker**：注入/XSS/鉴权绕过、数据损坏、竞态、破坏 API 契约、关键路径无错误处理
- **Should fix**：缺校验、难懂逻辑、重要行为无测试、N+1、应抽取的重复
- **Nit**：无 linter 覆盖的风格、小命名、文档缺口

## 写法

1. 先给摘要：整体印象、主要风险、做得好的点
2. 每条：**位置 + 为何 + 具体改法**（可带短代码片段）
3. 用「建议考虑…」而非命令式改稿
4. 一轮 review 给全量反馈，不要分多轮 drip

## 本仓库

- 改代码前已读相关文件；最小 diff
- 可跑时用 `run_project_check` / `pytest` / `ruff` 佐证，不编造输出
